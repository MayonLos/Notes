---
title: "组合逻辑电路"
type: concept
tags:
  - concept
  - digital
aliases:
  - Combinational Logic Circuits
  - 组合逻辑
sources:
  - raw/04-notes/2026-05-05-数字电路课堂笔记/IMG_20260413_115845.jpg
  - raw/04-notes/2026-05-05-数字电路课堂笔记/IMG_20260415_094101.jpg
  - raw/04-notes/2026-05-05-数字电路课堂笔记/IMG_20260416_202934.jpg
  - raw/04-notes/2026-05-05-数字电路课堂笔记/IMG_20260420_120000.jpg
last_updated: 2026-05-07
---

> **一句话定义**：任意时刻输出**仅取决于当前输入**，与电路历史状态无关（无记忆元件）的数字电路，由基本逻辑门搭建而成。

---

## 一、加法器

### 半加器（Half Adder）

**功能**：只计算两个1位二进制数相加，**不处理进位输入**。

| 输出 | 逻辑表达式 |
|:---|:---|
| 和位 S | `S = A ⊕ B` |
| 进位 Cout | `Cout = A · B` |

> [!warning] 局限性
> 半加器**不能处理来自低位的进位**，只能用于最低位（LSB）的加法。

### 一位全加器（Full Adder）

**功能**：增加**进位输入 Cin**，可以级联实现多位加法。

| 输出 | 逻辑表达式 |
|:---|:---|
| 和位 S | `S = A ⊕ B ⊕ Cin` |
| 进位 Cout | `Cout = A·B + (A⊕B)·Cin` |

### 多位全加器

| 改进方案 | 原理 | 延迟 |
|:---|:---|:---|
| **超前进位**（Carry Look-ahead） | 并行计算所有进位 | O(log N) |
| **进位选择**（Carry Select） | 预计算两种进位结果按实际选择 | O(√N) |

> [!warning] 行波进位延迟
> 延迟随位数线性增长：$t_{delay} = N \times t_{carry}$，高速场景必须改进。

---

## 二、数据选择器 MUX

**功能**：从多路输入中**选择**一路输出。

$$Y = \bar{S_1}\bar{S_0}D_0 + \bar{S_1}S_0 D_1 + S_1\bar{S_0}D_2 + S_1 S_0 D_3 \quad \text{（4选1）}$$

> [!note] MUX 实现任意逻辑函数
> MUX 的逻辑表达式是**最小项之和**，可将真值表值直接接入数据端实现任意函数。

**常见芯片**：74HC151（8选1）、74HC153（双4选1）

---

## 三、译码器

**功能**：将 n 位二进制输入译为 $2^n$ 条互斥输出线中的某一条。

$$Y_0 = \bar{A_1}\bar{A_0}, \quad Y_1 = \bar{A_1}A_0, \quad Y_2 = A_1\bar{A_0}, \quad Y_3 = A_1 A_0 \quad \text{（2-4译码）}$$

> [!tip] 译码器 = 最小项发生器
> 译码器 + OR 门 = 任意组合逻辑函数

**常见芯片**：74LS138（3-8译码器，含3个使能端）

---

## 四、编码器

**功能**：将 $2^n$ 条输入线编码为 $n$ 位二进制输出。

| | 基本编码器 | 优先编码器 |
|:---|:---|:---|
| 输入约束 | 同时只能有1个输入有效 | 允许多个输入同时有效 |
| 冲突处理 | 多输入有效→输出不确定 | 响应**优先级最高**的输入 |

**常见芯片**：74LS148（8-to-3 优先编码器）

---

## 五、竞争与冒险

> 由于不同路径**信号传播延迟不同**，输入变化瞬间输出端可能出现短暂错误脉冲（毛刺/Glitch）。

> [!important]
> **竞争**是原因，**冒险**是结果。有竞争不一定有冒险，但有冒险一定有竞争。

| 消除方法 | 原理 |
|:---|:---|
| **增加冗余项** | 在相邻分组间添加覆盖两者的冗余乘积项 |
| **滤波电容** | 输出端加小电容滤除窄脉冲 |
| **时钟采样** | 只在时钟有效沿采样，忽略过渡期毛刺 |

---

## 六、基于 MSI 的组合逻辑设计

**MSI 设计思路**：利用中规模集成电路（MUX、译码器、加法器）的固有逻辑结构，通过**配置其数据/控制端**实现任意组合逻辑函数。

| 方法 | 额外门数 | 适用场景 |
|:---|:---|:---|
| MUX 直连法 | 0 | 变量数 ≤ MUX 地址数 |
| MUX 降维法 | 少量 | 变量数 = MUX 地址数 + 1 |
| 译码器 + NAND | 1 个 NAND 门 | 任意 n ≤ 译码器输入数 |

### 可控加减法器（74LS283 应用）

用 **74LS283（4 位超前进位加法器）** + XOR 门实现加减法切换：
- 加法模式（M=0）：$B_i$ 直通 → $S = A + B$
- 减法模式（M=1）：$B_i$ 取反 + $C_0=1$ → $S = A - B$（补码减法）

---

## 关联连接

- [[原码反码补码]] — 补码运算是加法器处理有符号数的基础
- [[逻辑函数化简]] — 设计核心步骤
- [[逻辑门电路]] — 物理实现层
- [[锁存器和触发器]] — 组合逻辑的记忆性扩展（时序逻辑）
- [[基础语法]] — C++ 中的位运算符（`&`、`|`、`^`、`~`、`<<`、`>>`）直接对应逻辑门操作
- [[摘要-数字电路课堂笔记]] — 来源
